cursor_trail 3

# Platform-specific settings are selected by scripts/bootstrap-kitty.sh.
# Linux receives an empty overlay; macOS receives kitty-platform.conf with the
# Option-as-Alt setting below.
include kitty-platform.conf
